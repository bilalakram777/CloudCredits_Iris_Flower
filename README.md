Iris Flower Classification
This project classifies iris flowers into species using Decision Tree and Logistic Regression models. It reads data from iris.csv, encodes labels, splits the data, trains both models, and evaluates them with accuracy and confusion matrix visualizations.

 Requirements
pandas

scikit-learn

matplotlib

seaborn
